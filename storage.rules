rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Private profile images are proxied through the authenticated application.
    match /{object=**} {
      allow read, write: if false;
    }
  }
}
