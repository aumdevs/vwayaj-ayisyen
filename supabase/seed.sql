-- Safe local seed.
-- Required reference data is installed by migration 0010.
-- Add only synthetic development records here. Never copy production data.
select 'reference seed already applied' as status;
